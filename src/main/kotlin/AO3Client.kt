/**
 * The main access point to AO3. Try to initialise only one (1) instance of this class.
 * Orpheus relies on Retrofit (and its dependencies) under the hood. Therefore, you should pass in your instances of
 * OkHttpClient.
 */
class AO3Client {
}